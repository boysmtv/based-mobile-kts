apply from: "$rootProject.rootDir/buildConfig/common-config.gradle"

dependencies {
    implementation fileTree(dir: 'libs', includes: ['*.jar'])
    implementation Core.coreKtx
    implementation Core.appcompat
    implementation ExternalLib.timber
    implementation Core.biometric
    implementation project(UtilitiesModules.utilitiesConstants)
}